export interface ContractStatus {
  id: number;
  code: string;
  name: string;
}

export interface InterestCalculationType {
  id: number;
  code: string;
  name: string;
}

export interface RentAdjustmentType {
  id: number;
  code: string;
  name: string;
}
